{
  "name": "json2canvas-project",
  "version": "0.0.0",
  "description": "Converts JSON specification into Canvas",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "base64-img": "^1.0.4",
    "cors": "^2.8.5",
    "express": "^4.14.0",
    "file-base64": "^1.0.0",
    "jsoneditor": "^7.0.4"
  }
}
